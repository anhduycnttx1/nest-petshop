import { Center, Loader } from '@mantine/core'

const LoaderPage = () => {
  return (
    <Center w="100%" h={300}>
      <Loader size="sm" color="cyan" />
    </Center>
  )
}

export default LoaderPage
